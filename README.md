## Build Setup:

``` bash
# Download repository:
git clone https://github.com/vorobel/marketika

# Go to the app:
cd marketika

# Install dependencies:
npm install

# Server with hot reload at http://localhost:8081/
npm run dev

# Output will be at dist/ folder
npm run build
```

